# name: Release Please

# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:

# permissions:
#   contents: write
#   pull-requests: write

# jobs:
#   release-please:
#     runs-on: ubuntu-24.04
#     steps:
#       - name: Run Release Please
#         uses: googleapis/release-please-action@c2a5a2bd6a758a0937f1ddb1e8950609867ed15c # v4.3.0
#         with:
#           # Use a PAT instead of the default token to enable workflows to run on the autogenerated PR
#           token: ${{ secrets.PAT_TOKEN }}
#           release-type: simple
#           config-file: .github/utils/release-please-config.json
#           manifest-file: .github/utils/.release-please-manifest.json
          
name: Semantic Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    name: Release to GitHub
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Required to fetch all history and tags

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Semantic Release and Plugins
        run: |
          npm install -g semantic-release @semantic-release/commit-analyzer @semantic-release/release-notes-generator @semantic-release/github

      - name: Run Semantic Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx semantic-release