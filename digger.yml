generate_projects:
  blocks:
    - block_name: staging
      terragrunt: true
      root_dir: "01-terraform-infra/envs/staging/"
      workflow: default

workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - init
        - apply